# Set provider specific settings here


#auth-nocache
dev @openvpniface@
script-security 2
persist-key
nobind
resolv-retry infinite

# insecures
# but this is a foreign option in linux and a useful one
#pull-filter ignore "dhcp-option DNS "
pull-filter reject "setenv "

#keepalive 3 10
# not updated anyhmore with a pull so a high value is initially set
ping-exit 60
ping 3
pull-filter ignore "ping-restart "
# tell systemd to reload services that depend on vpn
remap-usr1 SIGTERM

# Set log file verbosity.
verb 3

#CHANGE keep pw encrypted
#Should not be a problem when using alternate routing table
persist-tun

#mute-replay-warnings
# Silence repeating messages
mute 20

ifconfig-noexec
#for ip address assignment
# just print the cmd if vpn is tying to do somthing with our ip settings
iproute "@hookdir@/dummy-ip"
route-noexec

#pre route hooks
up "@hookdir@/updown.sh"
route-up "@hookdir@/updown.sh"
route-pre-down "@hookdir@/updown.sh"
down "@hookdir@/updown.sh"
